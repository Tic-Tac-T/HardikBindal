#include<iostream>
using namespace std;

int main(){
    // int size = 10, b[size];
    
    int b[size], size = 10;    return 0;
} 
//upper code which is commented out works properly but, the lower one is incorrect as the variable size should be declared first before using it futher
